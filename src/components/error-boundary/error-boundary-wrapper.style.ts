const textCenter = {
  display: 'flex',
  flexDirection: 'column',
  justifyContent: '',
  bgcolor: 'background.paper',
  borderRadius: 1,
  textAlign: 'center',
};

const boxProps = {
  width: 400,
  maxWidth: 500,
  margin: '100px auto',
};

const boxSecondaryProps = {
  display: 'flex',
  flexDirection: 'column',
  bgcolor: 'background.paper',
  borderRadius: 1,
};

export { textCenter, boxProps, boxSecondaryProps };
